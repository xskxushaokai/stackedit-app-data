## T1. 1965：【14NOIP普及组】珠心算测验#（洛谷P2141）

**时间限制: 1000 ms 内存限制: 131072 KB**

### 【题目描述】

珠心算是一种通过在脑中模拟算盘变化来完成快速运算的一种计算技术。珠心算训练，既能够开发智力，又能够为日常生活带来很多便利，因而在很多学校得到普及。

某学校的珠心算老师采用一种快速考察珠心算加法能力的测验方法。他随机生成一个正整数集合，集合中的数各不相同，然后要求学生回答：其中有多少个数，恰好等于集合中另外两个（不同的）数之和？

最近老师出了一些测验题，请你帮忙求出答案。

### 【输入】

输入共两行，第一行包含一个整数 n，表示测试题中给出的正整数个数。

第二行有 n 个正整数，每两个正整数之间用一个空格隔开，表示测试题中给出的正整数。

### 【输出】

输出共一行，包含一个整数，表示测验题答案。

### 【输入样例】
```
4
1 2 3 4
```
### 【输出样例】
```
2
```
### 【提示】

【样例说明】

由 1+2=3，1+3=4，故满足测试要求的答案为 2。注意，加数和被加数必须是集合中的两个不同的数。

【数据说明】

对于 100%的数据，3 ≤ n ≤ 100，测验题给出的正整数大小不超过 10,000。

### 【解析】

#### 解法一：
使用三层循环暴力枚举，范例代码
```cpp
#include<iostream>
using namespace std;

int main(){
    int n;
    cin>>n;
    int a[n], flag[n];
    for (int i=0;i<n;i++){
        cin>>a[i];
        flag[i] = 0;
    }

    int res = 0;
    // 遍历第一个加数
    for(int i=0;i<n;i++){
	    // 遍历第二个加数，注意，j从第i+1个数开始，避免a[i]+a[j]与a[j]+a[i]重复统计
        for(int j=i+1;j<n;j++){
            int summury = a[i]+a[j];
            // 遍历和
            for(int k=0;k<n;k++){
                if(k!=i && k!=j && a[k]==summury && !flag[k]) {
                    // 看清题目要求：然后要求学生回答：其中有多少个数，恰好等于集合中另外两个（不同的）数之和？
                    res++;
                    flag[k] = 1; // 将第k个数做标记，避免重复统计
                }
            }
        }
    }
    cout << res << endl;

    return 0;
}
```
<br>

#### 解法二：
借助哈希表，范例代码（by 饶哲轩）
```cpp
#include<iostream>
using namespace std;
bool has[10002];
int a[10002];
int n;
int main(){
	cin>>n;
	for(int i=1; i<=n; ++i){
		cin>>a[i];
		has[a[i]]=true;
	}
	int ans=0;
	// 遍历数列，将其作为待判断的两数之和
	for(int i=1; i<=n; ++i){
		int u=a[i];
		// j取值[1,u/2],判断u-j和j是否同时在数据a中
		// j取到u/2即停止，避免重复统计
		for(int j=1; j<=u/2; ++j){
			int k=u-j;
			// 判断条件：j和k同时在输入的数列中，且j不等于k
			if(has[j]&&has[k]&&j!=k){
				ans++;
				//cout<<j<<"+"<<k<<"="<<u<<"\n";
				break;
			}
		}
	}
	cout<<ans;
	return 0;
}
```


<br><br>

## T2. 1263：友好城市（洛谷T677374）

**时间限制: 1000 ms 内存限制: 65536 KB**

### 【题目描述】

Palmia国有一条横贯东西的大河，河有笔直的南北两岸，岸上各有位置各不相同的N个城市。北岸的每个城市有且仅有一个友好城市在南岸，而且不同城市的友好城市不相同。

每对友好城市都向政府申请在河上开辟一条直线航道连接两个城市，但是由于河上雾太大，政府决定避免任意两条航道交叉，以避免事故。编程帮助政府做出一些批准和拒绝申请的决定，使得在保证任意两条航线不相交的情况下，被批准的申请尽量多。

### 【输入】

第1行，一个整数N(1≤N≤5000)，表示城市数。

第2行到第n+1行，每行两个整数，中间用1个空格隔开，分别表示南岸和北岸的一对友好城市的坐标。(0≤xi≤10000)

### 【输出】

仅一行，输出一个整数，表示政府所能批准的最多申请数。

### 【输入样例】
```
7
22 4
2 6
10 3
15 12
9 8
17 17
4 2
```
### 【输出样例】
```
4
```

### 【解析】
城市的坐标为一维坐标，输入为 $[pos1, pos2]$ 格式的一对友好城市坐标（$pos1$为南岸城市坐标，$pos2$为北岸城市坐标），将每对友好城市的坐标存储在自定义结构体```city``` 中，因此使用```city```类型的数组```a```来存放所有城市对的坐标。

可以先将```a```数组（即友好城市对）按照```pos1```从小到大排序，题目要求最大的不交叉的航线数量，即寻找排序后的```pos2```序列的**最长上升子序列（LIS）**。

![输入图片说明](/imgs/2026-03-05/SZ1e7mcOdcuq3gYn.png)

代码如下：

```cpp
#include<iostream>
#include<algorithm>

using namespace std;

struct city{
    int pos1, pos2;
};

city a[5005];
int dp[5005];

bool cmp(city c1, city c2){
    return c1.pos1<c2.pos1;
}

int solve(int n){
    sort(a, a+n, cmp);

    int maxn = 0;
    // 按pos1排序后，对pos2找LIS
    for(int i=0;i<n;i++){
        dp[i] = 1;
        for(int j=0;j<i;j++){
            if(a[j].pos2<a[i].pos2) dp[i] = max(dp[i], dp[j]+1);
        }
        maxn = max(maxn, dp[i]);
    }

    return maxn;
    
}

int main(){
    int n;
    cin >> n;
    for(int i=0;i<n;i++) cin >> a[i].pos1 >> a[i].pos2;

    cout << solve(n) << endl;

    return 0;
}
```





<br><br>

## T3. 1238：一元三次方程求解（洛谷P1024）

**时间限制: 1000 ms 内存限制: 65536 KB**

### 【题目描述】

形如：ax3+bx2+cx+d=0 这样的一个一元三次方程。

给出该方程中各项的系数(a，b，c，d均为实数)，并约定该方程存在三个不同实根(根的范围在−100至100之间)，且根与根之差的绝对值≥1。要求由小到大依次在同一行输出这三个实根(根与根之间留有空格)，并精确到小数点后2位。

### 【输入】
一行，包含四个实数a，b，c，d，相邻两个数之间用单个空格隔开。

### 【输出】
一行，包含三个实数，为该方程的三个实根，按从小到大顺序排列，相邻两个数之间用单个空格隔开，精确到小数点后2位。

### 【输入样例】
```
1.0 -5.0 -4.0 20.0
```
### 【输出样例】
```
-2.00 2.00 5.00
```
### 【解析】
题目给的条件：**存在三个不同实根（根的范围在 $-100$ 至 $100$ 之间），根与根之差的绝对值 $\ge 1$**。这意味着，$[-100,100]$区间内的某个整数$i$，仅存在一个根属于区间$[i,i+1)$，这样的 $i$有三个，所以我们只需要将区间$[-100,100]$划分为长度为1的小区间，依次判断每个区间内是否存在一个根。

如何判断小区间内是否存在根呢？刚才我们已经根据题意推断出，小区间内至多只有$1$个根，如果某区间有且仅有$1$个根（$f(x)=0$），则这个区间的左右两个端点处的函数值符号必定相反（$f(left)*f(right)<0$）。故我们仅需要在满足$f(left)*f(right)<0$的小区间内寻找根即可。小区间内寻找根直接使用**实数二分查找**即可，判断$f(mid)$值与$f(left)$、$f(right)$符号是否相异（即$f(mid)$与$f(left)$、$f(right)$的乘积是否小于$0$），将根的查找范围缩小到符号相异的小区间内即可。直到查找区间的长度缩小到满足精度要求（**题目要求精确到0.01，则查找时的区间需要缩小到0.001以内，四舍五入之后才能精确到0.01**），则端点值就是我们要找的根。故此题为**实数二分查找**。

**完整代码**

```cpp
#include<iostream>
using namespace std;

double a,b,c,d;
double fun(double x)
{
    return a*x*x*x+b*x*x+c*x+d;
}

double bin_search(double l, double r){
    // 实数二分，左闭右开，找到一个即可提前返回，否则缩小查找区间，直到满足精度要求
    while(r-l>0.001){
        double mid = (l+r)/2.0;
        double f_mid = fun(mid);
        
        // mid恰好为一个精确根，则直接return mid
        if(f_mid==0) return mid;
        // fun(mid)与fun(left)异号，表明根在[left,mid)之间，后续查找需要缩小右边界
        else if(f_mid*fun(l)<0) r = mid;
        // 否则，根在(mid,right]之间，后续查找需要缩小左边界
        else l = mid + 0.001;
    }
    return l;
}

int main(){
    cin >> a >> b >> c >> d;

    // -100到100之间枚举所有端点值
    double left, right, res;
    left = fun(-100);
    for(int i=-99;i<=100;i++){
        right = fun(i);
        // 特判左端点是否为根，注意相邻区间公用同一个端点，故只需要判断左端点即可，避免重复判断
        if(left==0) printf("%.2f ", (double)i-1);    
        // 若左右端点函数值异号，则在[i-1,i)区间查找根
        else if(left*right < 0){   
            res = bin_search((double)i-1, (double)i);  // 注意数据类型
            printf("%.2f ", res);
        }

        left = right;    // 当前右端点成为下一个区间的左端点
    }

    return 0;
}
```

<br><br>

## T4. 1221：分成互质组
**时间限制: 1000 ms 内存限制: 65536 KB**

### 【题目描述】
给定n个正整数，将它们分组，使得每组中任意两个数互质。至少要分成多少个组？

### 【输入】
第一行是一个正整数n。1 ≤ n ≤ 10。
第二行是n个不大于10000的正整数。

### 【输出】
一个正整数，即最少需要的组数。

### 【输入样例】
```
6
14 20 33 117 143 175
```
### 【输出样例】
```
3
```

### 【解析】

[**深搜解法 参考视频讲解**](https://www.bilibili.com/video/BV1144y1K7ra/?share_source=copy_web&vd_source=112561ecfd81a3ee5bf3c71dbb15e4b7)

<iframe src="//player.bilibili.com/player.html?isOutside=true&aid=979751828&bvid=BV1144y1K7ra&cid=552365521&p=1" scrolling="no" border="0" frameborder="no" framespacing="0" allowfullscreen="true"></iframe>

**贪心解法 参考代码**（by 刘辰宇）：
```cpp
#include<bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin>>n;
    vector<int> nums(n+1);
    for(int i=1;i<=n;i++) {
    	cin>>nums[i];
	}
	
	vector<set<int>> groups;
	groups.push_back({nums[1]});
	
	for(int i=2;i<=n;i++) {
		bool flag=true;
		for(auto &j:groups) {
			flag=true;
			for(auto &k:j) {
				if(__gcd(nums[i],k)!=1) {
					flag=false;
					break;
				}
			}
			if(flag) {
				j.insert(nums[i]);
				break;
			}
		}
		if(!flag) {
			groups.push_back({nums[i]});
		}
	}
	cout<<groups.size();
}
```
<!--stackedit_data:
eyJoaXN0b3J5IjpbMTE0NDUyMjQ0MCwxNDI2NTQ2MzYzLDEzOT
QwMzg4NjcsMTkxOTM1MzA2NCwxNzIxNzg0NzkzLDE1OTYyNzA3
MzAsMTA4NzM2NjkyMiwxMTE2ODI3NDIxLC0xNjkyMTY4NTgzLD
E0Mjc2MDI4NjgsMTc0NDgwNzAwMCw4MjE1MjE3NTUsLTQwMDgw
MTUzLC0xNjc1MDM3NjMxLC0xOTQyMDM3MDUxLC01ODAxMTk2Ny
wxODE3NjQ5MjA1LDk0MTM2MzAyLC0yMDk1Mjc4NTk1LC0zMzEz
MTExODZdfQ==
-->