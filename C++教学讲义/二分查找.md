# 二分查找

## lower_bound
**查找第一个等于或大于x的元素**

## upper_bound
**查找第一个大于x的元素**

```cpp
#include<algorithm>

int idx_l = lower_bound(a, a+n, x) - a  // 查找第一个大于等于x的元素
int idx_u = upper_bound(a, a+n, x) - a  // 查找第一个大于x的元素

if(a[idx_l]==x) // 第一个等于x的元素

// 查找最后一个等于x的元素
a[idx_u-1]==x

// 查找最后一个等于或小于x的元素
idx_u - 1

// 查找最后一个小于x的元素
idx_l - 1

// 计算序列中x的个数
idx_u - idx_l

```
<!--stackedit_data:
eyJoaXN0b3J5IjpbLTg5NDEyMjU4MywtMzYxOTY4MTI5XX0=
-->