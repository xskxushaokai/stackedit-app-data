# 二分查找

## lower_bound
```cpp
#include<algorithm>
```
**lower_bound() 查找第一个等于或大于x的元素**

## upper_bound
**查找第一个大于x的元素**

```cpp
int idx = lower_bound(a, a+n, x) - a  // 查找第一个大于等于x的元素

if(a[idx]==x) // 第一个等于x的元素


```
<!--stackedit_data:
eyJoaXN0b3J5IjpbLTEzNjYxOTQwNTBdfQ==
-->